# 朗诵情感图谱 - AI Studio 上下文说明

---

## ⚠️ 重要声明（必须遵守）

这是一个 **已经存在的 React + Vite 项目**。

你当前任务不是创建新项目，而是：

👉 在现有代码基础上进行 UI 优化

---

## ❌ 严格禁止行为

你不能：

- ❌ 重新生成项目结构
- ❌ 创建新的 UI 框架
- ❌ 重写 App 架构
- ❌ 替换现有分页逻辑
- ❌ 修改语势计算逻辑
- ❌ 修改 token / parse / layout 系统
- ❌ 改变数据结构

---

## ✅ 允许行为

你只能：

### UI 层优化
- 修改 styles.css
- 调整布局 spacing
- 优化字体 / 颜色 / 层级
- 优化卡片视觉
- 优化 header 视觉
- 优化语势图显示

---

## 🧠 项目定位

这是一个：

👉 A4 打印级「朗诵情感图谱生成系统」

核心能力：

- 拼音层（辅助发音）
- 文稿层（正文内容）
- 语势层（情绪曲线）
- 分页系统（打印结构）

---

## 📌 技术结构

### 前端技术栈：

- React
- Vite
- TypeScript（部分）
- CSS Layout system

---

## 🚪 入口文件

所有 UI 渲染入口：

---

## 🧱 核心结构（必须保持）

页面结构：
Header（标题区）
↓
Page Container（A4页面）
↓
Card List（每一行）
├── 拼音层
├── 文稿层
└── 语势层
---

## 📄 核心规则（非常重要）

### 1. 分页系统
- 由 smartPaginate 控制
- ❌ 不允许修改

---

### 2. 拼音系统
- 自动生成
- ❌ 不允许修改逻辑
- 只允许调整 UI 样式

---

### 3. 语势系统
- 基于曲线数据渲染
- ❌ 不允许改算法
- 只允许改视觉表现

---

## 🎨 UI 优化目标

你当前任务是：

👉 把 UI 优化为「A4打印级朗诵讲义」

风格要求：

- 国风 + 教学排版
- 清晰层级结构
- 可打印友好
- 非 dashboard 风格
- 非 Web SaaS 风格

---

## 🧩 UI 可修改范围

### Header
- 主标题（书法风格）
- 副标题
- 标签按钮

---

### Card System
- 卡片边距
- 阴影
- 圆角
- 分层间距

---

### Typography
- 字体大小
- 字重
- 行高
- 层级

---

### Prosody Layer
- 曲线颜色
- 填充透明度
- 背景层
- 视觉平滑度（仅UI）

---

## 🚫 UI禁区（绝对禁止）

- 不允许改变结构逻辑
- 不允许改 layout engine
- 不允许改数据解析
- 不允许改分页策略
- 不允许重建组件系统

---

## 🎯 最终目标

请把当前系统优化成：

> 一个可以打印的「朗诵教学图谱」

而不是：

> 一个 UI Dashboard 或数据可视化系统

---

## 📦 修改原则

始终遵循：

1. 保持结构稳定
2. 只做视觉优化
3. 不改变数据逻辑
4. 不改变渲染流程
5. UI 与逻辑严格分离

---

## 🧠 开发者提示

如果你不确定：

👉 优先选择“不改逻辑，只改样式”

---

# END