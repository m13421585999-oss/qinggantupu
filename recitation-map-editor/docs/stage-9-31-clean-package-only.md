# Stage 9.31 · 工程清洁包

本阶段只做工程清洁，不新增功能，不修改 A4 排版逻辑，不修改交互逻辑，不修改视觉样式。

## 处理内容

- 移除 `node_modules/`。
- 移除 `dist/` 构建产物。
- 移除 macOS 元数据：`.DS_Store`、`__MACOSX/`、`._*`。
- 新增 `.gitignore`，避免后续重复打包本地依赖、构建产物和系统元数据。
- 更新 `app/README.md`，使标题与当前 `package.json` 版本保持一致，并补充清洁安装与打包规则。

## 保持不变

- 不新增导入 / 导出功能。
- 不新增 Markdown 解析功能。
- 不修改 token 数据结构。
- 不修改分页、语势、拼音、点击编辑逻辑。
- 不修改 UI 视觉样式。
